apiVersion: v1
kind: Service
metadata:
  name: redis-master-external
  namespace: skillsier
  labels:
    app: redis
    role: master
spec:
  type: NodePort
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30379  # Fixed port for Redis
  selector:
    app: redis
    role: master