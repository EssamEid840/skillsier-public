apiVersion: v1
kind: Secret
metadata:
  name: users-be-postgres
  namespace: skillsier
  labels:
    app: users-be
    component: database
type: Opaque
stringData:
  POSTGRES_USER: "usersuser"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  POSTGRES_DB: "usersdb"
---
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-client-users-be
  namespace: keycloak
  labels:
    app: users-be
    component: auth
type: Opaque
stringData:
  client-id: "users-be"
  client-secret: "CHANGE_ME_IN_PRODUCTION"
---
# Note: kafka-admin-credentials should already exist in kafka namespace
# Created by Kafka installation. This is just a reference.
# 
# To get the password:
#   kubectl get secret admin-user -n kafka -o jsonpath='{.data.password}' | base64 -d
#
# If you need a service-specific Kafka user, create a KafkaUser CR:
#
# apiVersion: kafka.strimzi.io/v1beta2
# kind: KafkaUser
# metadata:
#   name: users-user
#   namespace: kafka
#   labels:
#     strimzi.io/cluster: kafka-cluster
# spec:
#   authentication:
#     type: scram-sha-512
#   authorization:
#     type: simple
#     acls:
#       - resource:
#           type: topic
#           name: users-
#           patternType: prefix
#         operations: [Create, Write, Read, Describe]
#       - resource:
#           type: group
#           name: users-
#           patternType: prefix
#         operations: [Read, Describe]