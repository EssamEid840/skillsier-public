apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: users-consumer-kc-admin
spec:
  pubsubname: kafka-pubsub
  topic: keycloak.admin-events
  routes:
    default: /events/keycloak/admin
scopes: [users-consumer]
