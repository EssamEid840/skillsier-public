apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: users-consumer-kc-realm
spec:
  pubsubname: kafka-pubsub
  topic: keycloak.realm-events
  routes:
    default: /events/keycloak/realm
scopes: [users-consumer]
